[project]
name = "comfyui-badmannodes"
description = "Utility nodes including ImageBlend, Math, Strings, Color tools (LAB transfer, Color Match w/ presets, Chroma Clean), Wildcard Processor, Latent Noise Mask, Tiled Background Removal, and WAN Three Frame To Video for keyframe-based video generation"
version = "1.3.0"
license = "LICENSE"

[project.urls]
Repository = "https://github.com/MariusKM/ComfyUI-BadmanNodes"
#  Used by Comfy Registry https://comfyregistry.org

[tool.comfy]
PublisherId = "bigting"
DisplayName = "ComfyUI-BadmanNodes"
Icon = ""
#adding stuff to trigger action